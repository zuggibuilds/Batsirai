version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: batsirai
      POSTGRES_USER: batsirai
      POSTGRES_PASSWORD: batsirai_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  minio:
    image: minio/minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: batsirai
      MINIO_ROOT_PASSWORD: batsirai_dev
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

volumes:
  postgres_data:
  minio_data:
